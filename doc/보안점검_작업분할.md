# 2026-09-19 전체 점검 — 작업 분할 목록

> 2026-09-19에 하드코딩/상수화, 미구현·임시구현, 과금·보안 허점 세 관점으로 전체 코드베이스를 점검한 결과를 실행 가능한 작업 단위로 쪼갠 문서. 이 문서를 읽는 세션/에이전트는 이 파일만 보고도 각 작업을 독립적으로 진행할 수 있어야 한다. `tayeon-a9` 세션이 컨트롤타워 역할로 배분·검증하고, 실제 구현은 다른 Claude 세션(`tayeon-fc` 등)과 Codex가 병렬로 진행한다.

**작업 시작 전 공통 규칙**
- Git 브랜치: `main`이 개발, `live`가 운영(App Hosting이 `live`를 배포). 절대 `live`에 직접 push하지 않는다. `main`에 커밋 → 사용자 확인 후 `live`로 병합.
- `admin/`은 완전히 분리된 앱이다. Tayeon 본체 라우트로 합치지 말 것.
- 수정 후 최소 `npx tsc --noEmit` 통과 확인. 로컬 dev 서버는 직접 실행하지 말 것(사용자가 확인).
- 작업 완료 시 이 문서의 해당 행 `상태`를 `완료`로 바꾸고, 무엇을 어떻게 고쳤는지 `비고`에 한 줄 남길 것. `담당` 세션 이름도 명시.

---

## 우선순위 1 — Critical/High (보안·과금)

### T1. [Critical] 가입/추천 보너스 무한 파밍 방지
- **담당**: `tayeon-fc`
- **관련 파일**: `src/app/api/auth/kakao/callback/route.ts` (특히 59행, 69-95행), `src/app/api/user/delete/route.ts` (13-14행), `src/lib/referral/code.ts` (`grantSignupFreePass` 59-77행, `grantSignupReferralReward` 83-117행)
- **문제**: uid가 `kakao:${카카오ID}`로 결정적이고, 가입 보너스 지급 여부 판단이 "`users/{uid}` 문서 존재 여부"뿐이다. `/api/user/delete`가 `recursiveDelete` + Firebase Auth 계정 삭제로 유저 문서를 완전히 지우므로, 같은 카카오 계정으로 탈퇴→재로그인을 반복하면 가입 무료 이용권(`SIGNUP_FREE_PASSES = 4`, `src/lib/tarot/pricing.ts:39`)이 매번 재지급된다. 추천 보상도 지급 이력이 추천인 자신의 문서 하위(`referralGrants`)에만 있어 같은 방식으로 재파밍 가능.
- **해결 방향**: 가입/추천 보너스 지급 이력을 유저 문서 트리 밖의 영구 컬렉션(예: `signupGrants/{kakaoId}`, `referralGrants/{referrerKakaoId}/{newUidOrKakaoId}` — 유저 서브컬렉션이 아닌 최상위 컬렉션)에 기록하고, 탈퇴로 삭제되지 않게 한다. 지급 전에 이 컬렉션을 먼저 확인.
- **완료 조건**: 같은 카카오 계정으로 탈퇴→재가입을 반복해도 가입/추천 보너스가 두 번째부터는 지급되지 않음을 코드로 확인(가능하면 실제 재현 또는 단위 테스트). `npx tsc --noEmit` 통과.
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(`tayeon-fc`): 지급 이력을 users/{uid} 서브트리 밖 최상위 컬렉션으로 이전. `signupGrants/{uid}` 문서로 가입 무료 이용권 지급 여부를, `referralGrants/{referrerUid}`(+`friends/{newUid}` 서브컬렉션, `friendsInvited` 카운터)로 추천 보상 지급 여부·인원수 상한을 각각 추적해 `/api/user/delete`의 recursiveDelete로는 지워지지 않게 함. `users/{uid}.referralSignupFriends` 필드는 폐기하고 `/api/referral/me`는 `getReferralInvitedFriendsCount()`로 새 컬렉션을 읽도록 변경. `npx tsc --noEmit` 통과 확인. Firestore 보안규칙은 이미 전체 default-deny라 신규 컬렉션도 클라이언트 접근 불가(추가 규칙 불필요).
- **검증**(tayeon-a9): diff 직접 리뷰 + `src/app/api/user/delete/route.ts` 확인해 `recursiveDelete`가 `users/{uid}`만 대상이라 신규 최상위 컬렉션이 실제로 탈퇴에 안 지워짐을 재확인. `firestore.rules` 직접 열어 전체 `allow read, write: if false` default-deny 주장도 사실 확인. `npx tsc --noEmit` 재실행 통과.

### T2. [High] 리딩 API 비용 방어 — 레이트리밋 + 질문 길이 제한
- **담당**: `tayeon-fc`
- **관련 파일**: `src/app/api/tarot/reading/route.ts`
- **문제**: 동시요청 잠금(60초, `READING_LOCK_STALE_MS`)만 있고 분당/일당 횟수 제한, 질문 글자수 제한이 전혀 없다. 유효 이용권만 있으면 순차 반복 호출로 Anthropic 비용을 무제한 유발 가능.
- **해결 방향**: 후보값은 `doc/작업현황.md`의 "비용 방어" 절 참고(질문 최대 1,000자, 분당 10회·24시간당 100회 등 — 확정치 아님, 합리적 기본값으로 구현 후 조정 가능하게 상수화). UID 기준 서버 집계(Firestore 카운터 또는 유사 메커니즘)로 제한하고, 초과 시 재시도 가능 시각을 포함한 429 응답. 기존 60초 락과 공존시킬 것.
- **완료 조건**: 제한 초과 시 429 응답과 Claude 미호출·이용권 미차감을 확인. 정상 사용 시나리오(1~2회 리딩)는 영향 없음을 확인. `npx tsc --noEmit` 통과.
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(`tayeon-fc`): `doc/작업현황.md` "비용 방어" 절 후보값(질문 1,000자, 분당 10회, 일 100회)을 상수화해 구현. 질문 길이는 요청 초입에서 400으로 즉시 거부(Claude 미호출). 분당/일당 카운터는 기존 60초 동시요청 락과 같은 트랜잭션(`acquireReadingLock`)에서 UID 기준 고정 윈도우 방식으로 증가·검사하도록 합쳐서, 두 번 왕복 없이 원자적으로 판단하고 초과 시 락도 걸지 않음(=Claude 호출·이용권 차감 전에 차단) — 재시도 가능 시각을 `retryAfterSeconds` + `Retry-After` 헤더로 429 응답에 포함. 정상 시나리오(1~2회 리딩)는 카운터가 한도 밖이라 기존 동작과 동일. `npx tsc --noEmit` 통과 확인. 실제 API 호출 재현 테스트는 로컬 dev 서버를 직접 띄우지 않는 규칙상 진행 안 함 — 사용자가 직접 확인 필요.
- **검증**(tayeon-a9): diff 직접 리뷰. 레이트리밋 걸릴 때 락/카운터를 안 건드리는지, `releaseReadingLock`이 `readingLockAt`만 지우고 분당/일당 카운터는 그대로 두는지(설계 일관성) 확인. `npx tsc --noEmit` 재실행 통과, 본체 `npm run build` 전체 통과도 확인.

---

## 우선순위 2 — Medium (하드코딩/정합성)

### T3. [High] 어드민 홈 시간제 지급 가격표 최신화
- **담당**: Codex
- **관련 파일**: `admin/src/app/page.tsx` (55-59행, 사용처 237/248-253/648행)
- **문제**: `TIME_PASS_TIERS`가 리팩터 이전 3단 가격(15분 8,900 / 30분 19,900 / 60분 35,900)을 그대로 쓰고 있다. 실제 12종 SKU(`src/lib/tarot/pricing.ts`의 `TIME_PASS_PACKAGES`, 조합 4종 × 15/30/60분, 8,900~65,900원)와 15분/타로 조합만 일치하고 나머지는 전부 틀린 가격·조합이다. 이 화면으로 지급하면 이용권에 잘못된 `priceWon`이 기록된다.
- **해결 방향**: 이 화면이 별도 API(`admin/src/app/api/admin/users/[uid]/grant-time-pass/route.ts`)를 쓰는지, T4에서 만들 공용 카탈로그를 참조하게 할지 확인 후 12종 SKU 선택 UI로 교체. T4와 함께 처리하면 중복 작업을 피할 수 있음 — 먼저 T4를 보고 진행 순서를 정할 것.
- **완료 조건**: 어드민 홈에서 시간제 지급 시 12종 SKU 전부 선택 가능하고 가격이 `pricing.ts`와 정확히 일치. `cd admin && npx tsc --noEmit` 통과.
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(Codex): 어드민 홈의 시간제 지급 선택지를 공용 12종 SKU로 교체하고 productId만 전송해 서버 카탈로그의 정확한 가격·조합으로 지급되게 수정; `cd admin && npx tsc --noEmit` 통과.
- **검증**(tayeon-a9): T4 재작업(아래) 반영 후 diff 재확인 — `TIME_PASS_TIERS`가 이제 `@/lib/timePassPackages`의 12-SKU에서 파생되고, 지급 요청도 `productId`만 보내 서버가 가격/조합을 결정. 구형 minutes 기반 요청도 하위 호환으로 남겨둠. `cd admin && npm run build` 실제 프로덕션 빌드 성공까지 확인(17개 라우트 정상 생성).

### T4. [Medium] `grant-time-pass` 카탈로그 중복 제거
- **담당**: Codex
- **관련 파일**: `admin/src/app/api/admin/users/[uid]/grant-time-pass/route.ts` (6-11행 `TIME_PRODUCTS`), `src/lib/tarot/pricing.ts` (214-227행 `TIME_PASS_PACKAGES`)
- **문제**: 어드민이 본체(`src/`)를 import할 수 없는 별도 앱이라 12개 SKU를 손으로 복제해 두었다(주석으로 의도는 밝혀져 있음). 본체 가격이 바뀌면 이 파일을 잊고 안 고칠 위험이 있다.
- **해결 방향**: 두 앱이 공유할 수 있는 방법을 검토 — (a) `pricing.ts`의 SKU 정의를 JSON/상수 파일로 빼서 두 앱이 각각 복사해 두되 빌드 타임에 동일한 소스에서 생성(간단한 codegen 스크립트)하거나, (b) 두 앱이 공유 가능한 workspace 패키지로 승격. 완전 자동 동기화가 과할 경우, 최소한 두 파일 상단에 "다른 쪽 수정 시 반드시 같이 수정" 주석 + 값 일치 여부를 CI에서 검증하는 간단한 테스트라도 추가. 구현 전 어느 방식으로 할지 간단히 판단해서 진행(과설계 금지, 이 프로젝트는 admin이 별도 앱이어야 한다는 제약이 있음 — 본체 라우트로 합치지 말 것).
- **완료 조건**: 가격/조합 변경 시 한 곳만 고치면 되거나, 최소한 불일치를 빌드/테스트에서 잡아낸다. `npx tsc --noEmit`(양쪽 다) 통과.
- **상태**: 완료 (재작업) — tayeon-a9 재검증 완료
- **비고**(`tayeon-fc`): Codex의 리포 루트 `shared/timePassPackages.ts` 공유안은 admin의 `turbopack.root` 고정(admin/next.config.ts) 때문에 `next build`가 `Module not found`로 실패해 반려됨 — `shared/` 폴더를 삭제하고 해결 방향 (a)안대로 되돌렸다. `src/lib/tarot/pricing.ts`의 `TIME_PASS_PACKAGES`/`ComboKey`를 원래대로 본체 안에 자체 정의로 복원하고, admin 쪽은 `admin/src/lib/timePassPackages.ts`를 새로 만들어 동일한 12-SKU 배열을 자체 보유(두 파일 모두 상단에 "다른 쪽 수정 시 반드시 같이 수정" 주석 추가). admin route(`grant-time-pass`)와 T3에서 고친 `admin/src/app/page.tsx`는 이제 `@/lib/timePassPackages`(admin 앱 내부 경로)를 import하도록 수정 — 리포 루트를 걸치는 경로 없음. `npx tsc --noEmit` 양쪽 통과는 물론, **`cd admin && npm run build`와 루트 `npm run build`(Turbopack 프로덕션 빌드) 둘 다 직접 실행해서 성공 확인함**(admin: 17개 라우트, 본체: 47개 라우트 정상 생성). T3는 이 수정으로 영향 없음(별도 재확인했고 admin 빌드 로그에 `/`, grant-time-pass 라우트 정상 포함).
- **검증**(tayeon-a9): `shared/` 디렉토리가 실제로 사라졌는지, admin이 `@/lib/timePassPackages`(admin 내부 상대경로) 하나만 참조하는지 diff로 직접 확인. `cd admin && npm run build`, 루트 `npm run build` 둘 다 직접 재실행해서 성공(Turbopack 에러 없음, admin 17개·본체 47개 라우트 정상 생성) 확인 완료.

### T5. [Medium] `ROOM_LIMIT` 이름 충돌 정리
- **담당**: Codex
- **관련 파일**: `src/app/api/tarot/rooms/route.ts:27` (`ROOM_LIMIT = 100`, 실제 계정당 대화방 상한), `src/app/api/user/usage-history/route.ts:6` (`ROOM_LIMIT = 30`, 이용내역 조회 시 스캔할 방 개수 — 의미가 전혀 다름)
- **해결 방향**: `usage-history/route.ts` 쪽 이름을 `USAGE_HISTORY_ROOM_SCAN_LIMIT` 등으로 변경. `rooms/route.ts`의 100 상한은 필요하면 공용 상수 모듈(`src/lib/tarot/...`)로 옮겨 다른 곳에서도 재사용 가능하게.
- **완료 조건**: 두 상수가 이름·값 모두 명확히 구분됨. `npx tsc --noEmit` 통과.
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(Codex): 이용 내역의 방 조회용 30개 제한을 `USAGE_HISTORY_ROOM_SCAN_LIMIT`으로 변경해 대화방 생성 상한 `ROOM_LIMIT`(100)과 의미를 분리; `npx tsc --noEmit` 통과.
- **검증**(tayeon-a9): diff 리뷰 완료, 본체 안에서만 일어난 변경이라 `npm run build` 전체 통과로 함께 확인됨.

### T6. [Low-Medium] 고객센터 첨부파일 제한 상수 공유화
- **담당**: Codex
- **관련 파일**: `src/app/api/support/inquiries/route.ts:8-9` (`MAX_ATTACHMENTS = 3`, `MAX_ATTACHMENT_BYTES = 2*1024*1024`), `src/components/SupportCenter.tsx:81-82` (같은 값 리터럴 재입력)
- **해결 방향**: 클라이언트/서버가 같은 Next.js 앱 안에 있으므로 `src/lib/support/constants.ts` 같은 공유 모듈을 만들어 양쪽이 import.
- **완료 조건**: 값이 한 곳에서만 정의됨. `npx tsc --noEmit` 통과.
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(Codex): 고객센터 첨부 개수·파일당 용량·총용량 제한을 `src/lib/support/constants.ts` 한 곳에 정의하고 클라이언트/서버가 함께 참조하도록 변경; `npx tsc --noEmit` 통과.
- **검증**(tayeon-a9): diff 리뷰 완료, 본체 안에서만 일어난 변경(admin 경계 안 건드림)이라 `npm run build` 전체 통과로 함께 확인됨.

---

## 우선순위 3 — Low (후순위, 정책 판단 필요하거나 스타일 수준)

### T7. [Medium, 정책 결정 필요 → 일부 High로 재분류 권고] 회원탈퇴 보관 정책 정비
- **담당**: tayeon-a9 (korean-law MCP로 법적 근거 조사 완료, 구현은 사용자 결정 대기)
- **문제**: `src/app/api/user/delete/route.ts`가 즉시 전체 `recursiveDelete`만 수행한다. `doc/작업현황.md`가 방침에 쓴 "대화 14일 보관, 결제기록 5년, 서비스 이용기록 3년"과 실제 동작이 다르다.
- **법률 조사 결과**(tayeon-a9, korean-law MCP, 2026-09-19):
  1. **결제기록 5년 — 법적 근거 확인됨, 현재 코드가 실제로 위반 중(신규 발견, High)**. 전자상거래법 시행령 제6조: 계약/청약철회 기록 5년, **대금결제 및 재화등의 공급에 관한 기록 5년**, 소비자 불만/분쟁처리 기록 3년, 표시광고 기록 6개월. 같은 법 시행령 제5조의2가 이 보존의무를 개인정보 보호법 제21조제1항(파기 원칙)의 명시적 예외로 지정해뒀다 — 즉 탈퇴 요청이 와도 이 기록은 지우면 안 된다. 그런데 `src/lib/payment/fulfill.ts:139`를 보면 결제 기록이 `users/{uid}/payments/{paymentId}`, 즉 **유저 문서 서브컬렉션**에 저장돼 있고, `/api/user/delete`의 `recursiveDelete(users/{uid})`가 이걸 통째로 지운다. 별도 아카이브 로직도 없음(`functions/src/index.ts` 확인). **결제한 적 있는 사용자가 탈퇴하면 그 순간 법정 보존기간 5년 미만인 대금결제 기록이 즉시 파기되는, 지금 당장의 실제 법 위반 상태**다. T1처럼 "farming 방지"가 아니라 순수 법 준수 문제라 우선순위를 올리는 게 맞다.
  2. **"서비스 이용기록 3년" — 문서 표현이 부정확함**. 전자상거래법 시행령 제6조의 3년 항목은 "소비자의 불만 또는 분쟁처리에 관한 기록"이지, 대화/리딩 내역 같은 일반 서비스 이용기록 전체를 3년 보관해야 한다는 법적 의무가 아니다(다른 법령에서도 근거를 찾지 못함). `doc/작업현황.md`의 이 문구는 정정이 필요하다 — 실제로는 "고객 문의·환불 등 분쟁 처리 기록만 3년" 정도가 정확한 근거고, 대화 내역 자체의 장기 보관은 법적 의무가 아니라 회사 재량.
  3. **"대화 14일 보관"**: 명시적 법적 근거를 찾지 못했다 — 계정 복구 유예기간 등 순수 운영 정책으로 보인다. 법 위반은 아니니 그대로 둬도 되지만, 방침 문서에서 "법적으로 이래야 한다"처럼 읽히지 않게 "운영 정책"으로 표현을 구분하는 게 정확하다.
  4. **T1에서 새로 만든 `signupGrants`/`referralGrants`(탈퇴해도 안 지워지는 최상위 컬렉션)의 개인정보 처리 근거**: 개인정보 보호법에 "부정이용 방지"를 이유로 파기를 유예해주는 명시적 조항은 못 찾았다(전기통신사업법의 "부정가입방지시스템"은 통신사 대상 별도 제도라 타연엔 적용 안 됨). 대신 제15조제1항제6호(개인정보처리자의 정당한 이익 — 명백히 정보주체 권리보다 우선하고 합리적 범위를 넘지 않을 것)를 근거로 수집·처리 자체는 정당화할 여지가 있어 보이지만, 이건 파기 예외가 아니라 별도의 수집·이용 근거라서 (a) 개인정보처리방침에 이 목적·항목·보유기간을 명시해야 하고 (b) 지금처럼 기간 제한 없이 영구 보관은 위험하다 — 합리적인 보유기간(예: 가입 후 N개월/N년)을 정해서 그 이후엔 마커도 파기하도록 설계해야 한다.
- **권고**: 4번(방침 문구 정정)과 2번(payments 서브컬렉션을 탈퇴 삭제 대상에서 분리 — 예: `users/{uid}/payments/*`를 탈퇴 전에 최상위 `paymentArchive/{paymentId}` 같은 곳으로 옮긴 뒤 나머지만 recursiveDelete)은 법 위반을 막는 것이므로 우선순위를 높여서 먼저 처리하는 걸 권장. `signupGrants`/`referralGrants` 보유기간 설정과 방침 문구 정정, "대화 14일"의 정확한 근거 표현 수정은 사용자가 보유기간·문구를 확정해주면 바로 반영 가능.
- **상태**: 완료 (사용자가 "바로 고쳐"로 확정, 코드 구현 완료 — TTL 정책은 배포 단계 남음, 아래 비고 참고)
- **비고**(`tayeon-fc`): 사용자가 확정한 두 항목을 구현.
  1. **결제기록 5년 보존**: `src/app/api/user/delete/route.ts`에서 `recursiveDelete` 호출 전에 `users/{uid}/payments/*` 문서 전부를 최상위 `paymentArchive/{paymentId}` 컬렉션(`src/lib/firestore/collections.ts`의 `PAYMENT_ARCHIVE`)으로 배치 복사 — 원본 필드에 `uid`(경로로만 암시되던 걸 명시적 필드로), `archivedAt`, `retainUntil`(paidAt 기준 +60개월, `addMonthsClamped` 재사용)을 추가. 복사가 끝난 뒤에만 기존처럼 `recursiveDelete`를 수행. `purchase-history`/`refund-requests`/`bonus-reward` 등 살아있는 계정의 기존 조회 경로는 `users/{uid}/payments`를 그대로 원본으로 쓰므로 변경 없음. 아카이브 자체의 5년 후 자동 파기 배치는 범위 밖(요청대로 과설계 금지 — 나중에 필요하면 스케줄 함수로 별도 추가).
  2. **signupGrants/referralGrants 마커 6개월 보유**: `src/lib/referral/code.ts`에 `markerExpiresAt()` 추가 — `signupGrants/{uid}` 문서와 `referralGrants/{referrerUid}/friends/{newUid}` 문서 양쪽에 생성 시각 기준 +6개월의 Firestore `Timestamp` 필드 `expiresAt`을 기록(TTL은 문자열이 아니라 실제 Timestamp 타입만 인식하므로 `Timestamp.fromDate` 사용). 요청대로 `referralGrants/{referrerUid}` **부모 문서**(`friendsInvited` 누적 카운터)에는 `expiresAt`을 넣지 않음 — 친구별 만료 시점이 달라 부모까지 TTL로 지워지면 카운터가 리셋되기 때문.
     - **배포 단계 필요(코드만으론 안 됨)** — Firestore 콘솔 또는 gcloud로 TTL 정책을 두 컬렉션(그룹)에 각각 활성화해야 실제로 6개월 후 자동 삭제가 동작한다. 프로젝트 ID는 `.firebaserc` 기준 `tayeon-d5149`, 기본(default) 데이터베이스 사용:
       ```
       gcloud firestore fields ttls update expiresAt \
         --collection-group=signupGrants --enable-ttl \
         --project=tayeon-d5149

       gcloud firestore fields ttls update expiresAt \
         --collection-group=friends --enable-ttl \
         --project=tayeon-d5149
       ```
       (`friends`는 컬렉션 그룹 지정이라 이름이 겹치는 다른 서브컬렉션이 있으면 같이 적용된다 — 지금 코드베이스 전체에서 `friends`라는 컬렉션명은 `referralGrants/{referrerUid}/friends`가 유일함을 grep으로 확인함.) Firebase 콘솔에서 하려면 Firestore Database → 해당 컬렉션 그룹 선택 → "TTL 정책" 탭에서 `expiresAt` 필드를 지정해도 동일. TTL은 활성화 후 실제 삭제까지 최대 72시간 걸릴 수 있음(Google 공식 문서 기준) — 즉시 사라지는 건 아님.
  - 검증: `npx tsc --noEmit` 통과 + **`npm run build`(루트) 실제 프로덕션 빌드 성공**(47개 라우트, 이번에도 확인 습관대로 실행). `src/lib/legal/content.ts`는 tayeon-a9가 직접 수정 중이라 건드리지 않음.
- **검증**(tayeon-a9): `delete/route.ts`·`referral/code.ts`·`collections.ts` diff 직접 리뷰 — 아카이브 복사가 `recursiveDelete`보다 먼저 실행되는 순서 확인, `retainUntil`이 `paidAt` 기준 60개월로 계산되는지 확인, `expiresAt`이 문자열이 아니라 `Timestamp.fromDate`로 만들어졌는지(TTL 요구사항) 확인, `referralGrants/{referrerUid}` 부모 문서(카운터)에는 `expiresAt`이 없는지 확인 — 전부 요청한 그대로 구현됨. `npx tsc --noEmit`, 루트 `npm run build`(47개 라우트) 재실행 성공. **TTL 정책(gcloud 명령어 2건)은 사용자 확인 후 tayeon-a9가 직접 실행함(2026-09-19)** — `signupGrants.expiresAt`, `friends.expiresAt` 둘 다 `ttlConfig.state: ACTIVE` 확인. 실제 자동 삭제는 활성화 후 최대 72시간 지연될 수 있음(Firestore TTL 특성).

### T8. [Low, 콘솔 승인 필요] 카카오 동의항목 확장
- **담당**: 보류
- **문제**: 현재 `account_email` 스코프만 요청(`src/app/login/page.tsx:18`). 생일/출생연도 저장 코드는 있으나 스코프가 없어 사실상 죽은 코드. 성별/연령대/전화번호는 코드 자체가 없음.
- **참고**: 카카오 디벨로퍼스 콘솔에서 해당 동의항목이 승인되기 전에는 코드를 먼저 만들어도 테스트 불가. 콘솔 승인 이후 착수.
- **상태**: 보류

### T9. [Low] Firestore 컬렉션명 문자열 상수화
- **담당**: Codex (여유 있을 때)
- **문제**: `"timePasses"`, `"countPasses"`, `"readings"` 등 컬렉션명이 14개 이상 파일에 리터럴로 반복. 현재 오타로 인한 실제 버그는 발견되지 않음 — 스타일/유지보수 차원.
- **완료 조건**: `src/lib/firestore/collections.ts` 등에 상수 모듈 추가하고 주요 경로 교체(전수 교체는 과하면 결제/이용권 관련 핵심 경로만 우선).
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(`tayeon-fc`): `src/lib/firestore/collections.ts` 신설 — `USERS`/`PAYMENTS`/`COUNT_PASSES`/`TIME_PASSES`/`ROOMS`/`READINGS`/`PENDING_REWARDS`/`REFERRAL_GRANTS`/`REFERRAL_CODES`/`SIGNUP_GRANTS`/`REFUND_REQUESTS`/`BIRTHDAY_COUPON_GRANTS`/`REFERRAL_GRANT_FRIENDS` 전부 정의. 전수 교체 대신 결제/이용권 핵심 경로 위주로 교체: `payment/fulfill.ts`, `payment/prepare`, `purchase-history`, `refund-requests`, `received-passes`, `pending-rewards`(+claim), `bonus-reward`, `referral/code.ts`, `tarot/reading`, `tarot/history`, `tarot/rooms`(+[roomId]), `tarot/time-pass/start`, `usage-history`, `user/me`, `user/delete`, `rewards/birthday.ts`, `auth/kakao/callback`(총 19개 파일). `user/partner`·`user/settings`·`user/signup`·`user/birth-info`·`auth/pg-review-login`은 순수 `users` 루트 참조만 있고 결제/이용권 서브컬렉션과 무관해 이번엔 리터럴로 남겨둠(향후 필요시 마저 교체 가능). `npx tsc --noEmit` 통과 + **`npm run build`(루트) 실제 프로덕션 빌드 성공**(47개 라우트) 확인.
- **검증**(tayeon-a9): `purchase-history`, `refund-requests` diff 리뷰 — 리터럴이 상수로 정확히 치환됐고 로직 변경 없음 확인. 루트 `npm run build` 재실행 성공(47개 라우트).

### T10. [Low] 환불 7일 기준 중복 표기 정리
- **담당**: Codex (여유 있을 때)
- **문제**: `src/app/api/user/purchase-history/route.ts:6`의 `REFUND_WINDOW_DAYS = 7`이 실제 판단 기준이고, 같은 숫자가 `src/app/(app)/charge/page.tsx`, `src/lib/legal/content.ts` 등 안내 문구에 그대로 재입력돼 있음.
- **해결 방향**: 안내 문구도 `REFUND_WINDOW_DAYS`를 import해서 템플릿 문자열로 넣도록 변경(약관 텍스트는 법률 검토 대상이라 숫자만 상수 참조로 바꾸고 문장 자체는 건드리지 않음).
- **상태**: 완료 — tayeon-a9 검증 완료
- **비고**(`tayeon-fc`): `REFUND_WINDOW_DAYS`를 `purchase-history/route.ts`에서 `src/lib/payment/refundPolicy.ts`(신규, firebase-admin 등 서버 전용 모듈 import 없음)로 옮겼다 — `charge/page.tsx`는 `"use client"` 컴포넌트라 route.ts를 직접 import하면 클라이언트 번들에 firebase-admin이 딸려 들어가 빌드가 깨지기 때문에, 값만 담은 별도 모듈이 필요했음. `purchase-history/route.ts`와 `refund-requests/route.ts`(별도로 자체 `WINDOW_MS = 7 * ...`를 하드코딩하고 있던 것도 같은 상수로 교체, 동일한 7일 규칙의 또 다른 중복이라 판단해 포함), `charge/page.tsx`(환불 안내 문구 2곳), `legal/content.ts`(제9조 환불 조항 본문, 숫자만 `${REFUND_WINDOW_DAYS}`로 치환)에서 이 값을 import해서 씀. `legal/content.ts`의 다른 "7일"(약관/개인정보처리방침 변경 시 사전 고지 기간, 제9조와 무관한 별개 규정)은 건드리지 않음. 문장 자체는 전부 그대로 두고 숫자만 템플릿 참조로 바꿈. `npx tsc --noEmit` + `npm run build` 둘 다 통과(`/charge`, `/terms`, `/privacy`, `/signup` 라우트 정상 생성 확인).
- **검증**(tayeon-a9): `refund-requests`, `purchase-history` diff 리뷰(위 T9 검증과 같이 확인) + 루트 `npm run build` 재실행 성공. `legal/content.ts` 제9조 diff도 확인 — 문장은 그대로 두고 숫자만 템플릿 치환됨.

---

## 참고 — 점검 중 확인된, 수정 불필요한 항목
- 결제 신뢰 경계(PortOne 재조회·금액검증·웹훅 서명검증), 이용권 차감 트랜잭션 동시성, IDOR, 어드민 인증(18개 라우트), 정지 사용자 차단(4개 지급 경로), 자미두수 태어난시간 서버검증(4개 경로) — 전부 견고하게 구현되어 있음을 확인.
- 대화방 100개 상한, 환불 요청 플로우(7일+미사용 서버 하드체크)는 `doc/작업현황.md`의 옛 "미착수" 문구와 달리 이미 완전히 구현되어 있음 — 작업현황 문서 정정 필요(별도 처리).

---

## 진행 로그
- 2026-09-19: `tayeon-a9`(컨트롤타워)가 하드코딩/미구현/보안 3개 관점 병렬 점검 완료, 이 문서로 작업 분할. `tayeon-fc`에 T1, T2 배정 요청.
