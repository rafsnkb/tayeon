/** 아직 한 번도 쓰지 않은 대화방의 제목.
 *
 *  방을 만들 때 이 값이 붙고(`/api/tarot/rooms`), 그 방에서 첫 리딩이 성공하면 서버가 질문
 *  앞부분으로 바꾼다(`/api/tarot/reading`). 그래서 이 제목이 그대로라는 건 곧 **아직 안 쓴 방**
 *  이라는 뜻이고, 여러 화면이 그 판정에 이 값을 쓴다 — 드로어의 "새 대화"가 빈 방을 새로 만들지
 *  않고 재사용할지, 대화방 탑바가 제목 대신 워드마크를 보여줄지, 이용 내역에 무엇을 적을지.
 *
 *  2026-09-26 이전엔 같은 문자열이 다섯 군데에 각자 박혀 있었다. 한쪽만 바꾸면 "안 쓴 방"
 *  판정이 조용히 어긋난다. */
export const DEFAULT_ROOM_TITLE = "새 대화";
