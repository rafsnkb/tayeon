// 결제 화면이 보여주는 환불(청약철회 제한) 안내 문구의 버전.
//
// **별 파일인 이유**: 화면(클라이언트 컴포넌트)과 서버가 같은 값을 써야 하는데, 서버 쪽 사용처인
// `purchase.ts` 는 `@/lib/payment/products` 를 타고 서버 전용 모듈로 이어진다 — 클라이언트에서
// import 할 수 없다. 그래서 상수만 의존성 없는 파일로 떼어 둔다.
//
// **문구를 고치면 이 값을 올릴 것.** 이 값의 존재 이유가 "그때 무엇을 보여줬는지"를 되짚는 것이다:
// 청약철회 제한을 주장하려면 제한사유의 존재와 **표시의무를 다했는지**를 둘 다 사업자가 증명해야
// 하고(대법원 2018다287034, 2023.6.15), 문구가 바뀌었는데 버전이 그대로면 결제 기록에 남은 버전으로
// 그 증명을 할 수 없다. 서버가 이 상수와 대조해서 옛 화면에서 온 동의를 거른다
// (`src/lib/saju/purchase.ts` 의 `validatePurchaseConsent`).
// ".2" (2026-09-26) — 문구를 목업 원문에서 법령 기준으로 고쳤다. 제한 근거를 전자상거래법
// 제17조제2항제5호(디지털콘텐츠 제공 개시)에서 **제6호**(주문에 따른 개별 생산)로 옮겼고,
// 환급 기산점을 "환불 사유 확인일"에서 **"청약철회등을 한 날"**(제18조제2항제2호)로 고쳤으며,
// 제17조제3항(표시·광고와 다른 경우)·미성년자 고지(제13조제3항)를 새로 넣었다. 바뀐 내용과
// 근거는 `src/app/(app)/fortune/[slug]/refundNotice.ts` 의 각 덩어리 주석에 있다.
// ".3" (2026-09-26) — 국가법령정보센터로 조문을 직접 대조해 두 곳을 고쳤다. ① 시행령 제21조가
// 요구하는 "회복할 수 없는 중대한 피해가 예상되는 경우" 요건이 문구에서 빠져 있어 고지+동의만
// 갖추면 제한되는 것처럼(법보다 넓게, 회사에 유리하게) 읽혔다 — 요건을 넣었다. ② 제17조제3항의
// 기산점 "안 날 또는 알 수 있었던 날" 중 "또는 알 수 있었던 날"이 빠져 있었다 — 넣었다.
// 근거는 `refundNotice.ts`의 ①·③ 주석과 `doc/사주_미리보기_설계.md` §0.
// ⚠️ 아직 **초안**이고 변호사 검토 전이다 — 최종본이 나오면 여기를 다시 올릴 것.
export const REFUND_NOTICE_VERSION = "2026-09-26.3";
